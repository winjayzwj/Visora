# 仅本地开发使用：将同源 /api/ 转发到已配置的 Visora API。
# 留空时 Vite 不创建代理，不会伪装成可用后端。
VISORA_API_TARGET=

# 仅独立本地开发需要：用户 Web 的精确 origin，例如 http://127.0.0.1:5173。
# 必须与 Admin 同 hostname/protocol（端口可不同）；同源部署可留空。
VITE_WEB_ORIGIN=
